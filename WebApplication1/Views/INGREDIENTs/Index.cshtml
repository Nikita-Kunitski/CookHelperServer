@model IEnumerable<WebApplication1.Models.INGREDIENT>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.name_ingredient)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UNIT_MEASURE.name_unit_measure)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.name_ingredient)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UNIT_MEASURE.name_unit_measure)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.C_id_ingred }) |
            @Html.ActionLink("Details", "Details", new { id=item.C_id_ingred }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.C_id_ingred })
        </td>
    </tr>
}

</table>
