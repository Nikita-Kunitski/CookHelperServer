@model IEnumerable<WebApplication1.Models.COOKING_STAGES>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.C_text)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BOOK_DISH.name_dish)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.C_text)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BOOK_DISH.name_dish)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.C_id_cooking_stages }) |
            @Html.ActionLink("Details", "Details", new { id=item.C_id_cooking_stages }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.C_id_cooking_stages })
        </td>
    </tr>
}

</table>
